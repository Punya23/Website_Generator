# website-generator

## Workflow

Always use gstack before making code changes — route through the relevant gstack skill
(e.g. `/investigate`, `/qa`, `/qa-only`, `/review`, `/ship`) instead of editing and
committing directly.
